<h1>Controller não encontrado</h1>
<p>Não foi possível encontrar o controller  <strong><?= $details["controller"] ?></strong>. 
    É possível que o arquivo <code>app/controllers/<?= $details["controller"] ?>_controller.php</code> não exista, 
    ou que a classe <strong><?= $details["controller"] ?></strong> não esteja definida.
</p>